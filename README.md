# kai-khun-bot

Simple Telegram bot that converts text messages to voice.

## Usage

1. Install requirements (`pip install python-telegram-bot gTTS`).
2. Set the environment variable `BOT_TOKEN` with your bot's token.
3. Run the bot with `python bot.py`.
